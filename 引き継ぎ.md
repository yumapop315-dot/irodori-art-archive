# 引き継ぎ書 — キヴォトスアーカイブ（ブルアカ画像サーチ）

最終更新: 2026-07-19 / 次のセッションはこのファイルを最初に読むこと。

---

## 1. これは何か

X(旧Twitter)に投稿されたブルーアーカイブのファンアートを、キャラ名タグで検索・閲覧できる**非公式まとめサイト**。
参考にした実在サイト = ブルアカ画像サーチ（https://kina-ko-m-ochi.net/image_search_allages/）。
画像は自前保存せず、Xの投稿情報を取得して自前カードで表示する方式（＝DBが軽い・権利面の建前を保てる）。

- 作業フォルダ: `C:\Users\shimadayuma\Desktop\Cload Code\画像サーチ`
- アプリ本体: `画像サーチ\bluearchive-search`（Next.js 16.2.10 / React 19 / TypeScript / Tailwind v4 / better-sqlite3）
- Node: v24.11.1
- 要件定義: `画像サーチ\要件定義.md`
- サイト名・URL: `彩りアートアーカイブ` / `https://irodori-art-archive.com`（**取得済み**。mixhostで取得したがDNSのみVPSに向ける方針）
- **サイト名の制約**: ブルアカ固有名詞(キヴォトス等)や公式タイトル語(Blue Archive)をサイト名・ドメインに使わない。公式誤認・不正競争防止法のリスク回避のため意図的にそうしている（旧名「キヴォトスアーカイブ」から改名済み）
- 本番サーバー: **クラウドVPS by GMO**（Ubuntu 24.04 / 1GB / 仮想2コア / SSD50GB / root権限あり / **アダルト可**）。手順は `デプロイ手順.md`
  - 国内VPSでアダルト可はここくらい。さくら/ConoHa/KAGOYA/WebARENAは規約でアダルト禁止。mixhostはアダルト可だが**共有サーバーでroot不可＝Node.jsが動かない**ので採用不可だった

## 2. 起動方法（重要）

現在は「開発サーバーをこのPC上で動かしている」だけ。**PC再起動でサーバーは止まる**（データは消えない）。

```
cd "C:\Users\shimadayuma\Desktop\Cload Code\画像サーチ\bluearchive-search"
npm run dev      # → http://localhost:3000
```

- 管理画面: http://localhost:3000/admin （パスワード = 環境変数 ADMIN_KEY、未設定時は `change-me`）
- **注意**: dev起動が二重になると `.next` キャッシュが壊れて500になることが何度かあった。おかしくなったら「port3000のプロセスをtaskkill → `.next`をリネーム退避 → 再起動」で回復する。`rm -rf`/`Remove-Item -Recurse` はこの環境のガードでブロックされるので `mv .next .next-broken-backup` で退避する運用。
- ブラウザ確認: mcp__Claude_Browser__preview_start に `{url:"http://localhost:3000"}` で開く。外部画像(pbs.twimg.com)はプレビューペインでブロックされるので実表示は実ブラウザで。

## 3. 実装済み機能（全部動作確認済み）

### 閲覧
- トップ/タグページ/絵師ページ = **50件ごとのページ送り**（`?page=2`）。以前は無限スクロールだったが本人希望でページ式に変更済み。PER_PAGE定数で件数変更可
- 軽量カード表示（画像はX直リンク+遅延読込）、画像クリックでライトボックス（複数枚めくり・キーボード操作）
- 6種ソート（登録順/投稿日順/人気順24h/人気順月間/最新いいね/ランダム）

### 検索（表記ゆれ対応）
- チップ式タグ入力+候補ドロップダウン。**前方一致**（「あ」→ア始まりのみ）
- ひらがな/ローマ字/フルネーム別名で解決。0件時は「もしかして」候補
- 単一タグは `/tag/キャラ名`、複数は `/?tags=A,B`（AND検索）。旧URL `/?tags=` も互換

### 3モード（健全/きわどい/R18）
- ヘッダー左のトグルで切替。R18は年齢確認・きわどいは表示確認ダイアログ。Cookieで30日保持
- 投稿は `posts.rating`（all/sensitive/r18）で区分。**サーバー側フィルタなので健全版に混ざる事故は構造的に起きない**
- 配色連動（健全=空色/きわどい=琥珀/R18=赤）。登録は現在のモードに入る。管理画面で振り分け変更可

### ユーザー機能（ログイン不要・localStorage）
- いいね（人気順に反映）+ お気に入り一覧 `/favorites`
- 生徒フォロー + 新着通知バッジ（ヘッダーのベル）+ `/following`
- 作者ミュート（カードの⋯）+ `/mutes`

### 登録
- URL貼付で自動プレビュー(600msデバウンス)→本文からタグ自動サジェスト→登録。一括登録(最大10件)
- **タグ未設定の投稿は公開側で非表示**（管理人がタグ付けした時点で公開＝実質承認制）
- センシティブ投稿対応: X公式APIが弾くため **FixTweet(api.fxtwitter.com)** にフォールバックして取得（lib/tweet.ts）
- IPベースのレート制限、重複・画像なしは拒否

### 管理画面 `/admin`
- パスワードログイン（httpOnly Cookie + HMAC署名。URLに鍵を載せない方式に変更済み）
- 投稿一覧: 左端に**公開状態バッジ**（緑=公開中+モード名/灰=非公開）、カード左縁の色分け、未保存時はオレンジ点滅の保存ボタン
- タグ編集: 確定タグ(青)と候補(緑枠)を区別表示、ひらがな候補入力
- モード振り分けプルダウン、削除、削除依頼の受付・対応管理
- **生徒(キャラタグ)マスタ管理** + **陣営(学園)の追加/削除**（StudentManager.tsx）
- 消えたツイート検出（画面ボタン or scripts/check-dead.mjs）

### 差別化機能（直近で追加）
- **ランキング** `/ranking`（週間TOP20・金銀銅バッジ）+ `/ranking/YYYY-MM`（月別アーカイブ）
- **関連タグ**: タグページに「◯◯とよく一緒に描かれる」共起タグ表示
- **絵師ページ** `/artist/screen_name`: 作者別まとめ。カードの作者名クリックで遷移

### SEO基盤（2026-07-19追加）
- `app/sitemap.ts` / `app/robots.ts`（健全版のタグ/絵師/月別ランキングを列挙。admin・api・個人用ページはクロール除外）
- OGP画像の動的生成: `app/opengraph-image.tsx`（サイト全体）+ `app/tag/[name]/opengraph-image.tsx`（タグ別・件数入り）。日本語フォントは`lib/ogFont.ts`が実行時にGoogle Fontsからサブセット取得、失敗時は英語表記にフォールバック
- JSON-LD: layoutにWebSite+SearchAction、タグページにCollectionPage+パンくず、/studentsにもパンくず（`components/JsonLd.tsx`）
- canonical一本化: ひらがな等の表記ゆれURL・`/?tags=単一タグ`は正規名の`/tag/◯◯`へ。複数タグは正規名の`/?tags=`へ
- タグページに「◯◯をよく描く絵師」TOP5（`topArtistsForTag`）、/studentsは陣営ジャンプナビ+件数付き索引に強化

### 表示順の重要な仕様（2026-07-19修正）
- 旧実装のCSS columns(段組み)は縦流しのため「古い投稿が上段に見える」問題があった。`components/Masonry.tsx`で行優先（左→右が新しい順）に振り分ける方式に変更。PostGrid/Feed両方が使用。SSR直後は段組みフォールバック→マウント後に行優先へ切替
- 登録順/投稿日順ソートに `p.id DESC` のタイブレークを追加（同時刻登録の順序を安定化）

### 広告
- AdSense実装済みだが**現在は無効**（env未設定なら枠ごと非表示）。健全版のみ配信する制御あり（成人向けはポリシー違反のため外さないこと）
- 有効化は `.env.local` に `NEXT_PUBLIC_ADSENSE_CLIENT` 等を設定するだけ。`.env.example` 参照

### DLsiteアフィリエイト（2026-07-19追加・成人向けモード専用）
- `lib/dlsite.ts` + `components/DlsiteSlot.tsx`。**きわどい版/R18版のみ**表示（`mode === "all"` なら必ずnullを返すので健全版に混ざらない＝AdSenseとの同居も構造的に起きない）
- 有効化は `.env.local` に `NEXT_PUBLIC_DLSITE_AFF_ID=<アフィリID>` を設定するだけ。未設定なら枠ごと非表示
- リンク形式は `NEXT_PUBLIC_DLSITE_AFF_TEMPLATE` で差し替え可（既定 `https://dlaf.jp/maniax/dlaf/=/aid/{aid}/url/{url}`）。DLsiteが `www.dlsite.com` → `dlaf.jp` にドメイン移行中で公式仕様が確定できなかったため、**実際に管理画面で生成したリンクと突き合わせて確認すること**（形式違いはエラーにならず成果が計上されないだけなので気づけない）
- 検索URLの実測仕様（外すと403）: 並び順は `order/trend` ではなく **`order[0]/trend`**、複数語の区切りは `%20` ではなく **`+`**
- 表示位置: タグページ/トップの下部バナー・インフィード（8件目以降12件ごと、健全版のAdSenseと同じ間隔）・ランキングページ。タグページでは**キャラ名連動**（「ホシノの同人作品をDLsiteで探す」）
- ステマ規制対応として `PR` バッジ + `rel="sponsored"` を付与済み。利用規約にもアフィリ利用を明記済み

## 4. コード地図

- `lib/db.ts` — 全DBスキーマ・マイグレーション・クエリ・生徒/陣営シード。**中心ファイル**
- `lib/site.ts` — サイト名/URL（変更はここ1箇所）
- `lib/mode.ts` — 3モードのCookie管理
- `lib/normalize.ts` — 表記ゆれ解決（かな/ローマ字/別名/前方一致）
- `lib/tweet.ts` — ツイート取得（公式API→FixTweetフォールバック）・エラー種別メッセージ
- `lib/adminAuth.ts` `lib/ratelimit.ts` `lib/clientStore.ts` `lib/paths.ts`
- `components/BrowsePage.tsx` — トップ/タグページの共通表示（検索・ソート・関連タグ・ページ送り）
- `components/PostGrid.tsx`（固定リスト用・広告差込あり）/ `components/Feed.tsx`（お気に入り/フォローの動的リスト用）
- `components/Pagination.tsx` `SearchForm.tsx` `TagSuggestInput.tsx` `PostCard.tsx` `ModeToggle.tsx` `StudentManager.tsx` ほか
- `app/` 配下に各ページ（page.tsx / tag/[name] / artist/[name] / ranking / ranking/[month] / admin / register / favorites / following / mutes / terms / removal / students）
- `scripts/check-dead.mjs`（消滅ツイート検出）`backfill-photos.mjs`（旧データの画像URL補完・使用済み）

## 5. 既知のTODO・注意点

### 掃除したほうがいいもの
- `components/LikeButton.tsx` は**未使用の残骸**（現在はPostCard内にいいねロジックがある）。削除してよい
- `bluearchive-search/.next-broken-backup` フォルダが残っている。手動削除推奨（自動削除はガードで不可）
- git はinit済みだが**コミットゼロ**。まだ一度もコミットしていない

### 本人が「今はいらない」と判断した長期TODO（蒸し返さない）
アカウント機能/自動バックアップ/BANリスト/承認制スイッチ/多言語化 等。聞かれるまで提案しない。

### 構造的リスク（把握だけしておく）
- **X依存**: react-tweet(syndication API)とFixTweetの両方に依存。どちらかが死ぬと新規登録が止まる（表示は画像直リンクなので生存）
- SQLiteはVPS前提。Vercel等サーバーレスでは永続化不可。R18運営もあるのでアダルトOKのVPS+Cloudflare無料が参考サイトと同構成（月600〜1000円）
- レートリミッタはメモリ内（複数プロセスでは効かない）
- DB容量は画像を持たないので軽い（10万投稿でも数百MB）。画像自前保存に変えると一変するので勧めない

### 提案済みで未着手の差別化案
案1「キャラ誕生日・イベント連動」（生徒マスタに誕生日列を足すだけ）は未実装。やる場合はここから。

## 6. 進め方の約束（ユーザーの好み）

- 実装依頼は**まとめて全部やる**スタイル。タスク分割して順に完遂→最後にE2E検証（ブラウザ/curl）まで確認する
- 変更後は必ず `npx tsc --noEmit` と動作確認。日本語で簡潔に結果報告
- ユーザーは相談→実装のサイクルで進める。質問（〜は本当？等）には実装せず調査で答える場面もあった
- 二次創作の権利・収益化リスクは正直に伝える方針で一貫している
- AGENTS.md注意: このNext.jsは破壊的変更あり版。コード書く前に `node_modules/next/dist/docs/` を参照（searchParamsはPromise等）
