# デプロイ手順 — 彩りアートアーカイブ

対象: クラウドVPS by GMO / Ubuntu 24.04 / メモリ1GB / root権限あり
ドメイン: `irodori-art-archive.com`（mixhostで取得、DNSのみVPSへ向ける）

上から順にコピペしていけば公開できる。**`【要変更】` の箇所だけ自分の値に置き換えること。**

---

## 0. 事前に手元で確認すること

- ✅ **VPSのIPアドレス**: `153.122.4.92`（本手順書に反映済み）
- **rootのパスワード** — GMOの開通通知メール、またはVPSポータルの「サーバーパスワード」メニューで確認/設定する
- mixhost管理画面で **DNSのAレコード**を編集できることを確認（解約前にやる）

---

## 1. SSHでサーバーに接続する

Windowsの PowerShell（または Windows Terminal）を開いて実行。追加ソフトは不要。

```powershell
ssh root@153.122.4.92
```

- 初回は `Are you sure you want to continue connecting?` と聞かれるので `yes`
- パスワードを入力（**入力しても画面には何も表示されない**が、打ててはいる）
- `root@xxxxx:~#` のような表示になれば接続成功

以降のコマンドは、**このSSHの画面（サーバー側）で実行する**。

---

## 2. 初期設定（更新・作業ユーザー・ファイアウォール）

### 2-1. パッケージを最新化

```bash
apt update && apt upgrade -y
```

（`Which services should be restarted?` のような画面が出たら Enter でそのまま進む）

### 2-2. 作業用ユーザーを作る

rootで常用するのは危険なので、アプリ用のユーザーを作る。

```bash
adduser app
```

パスワードを2回聞かれるので設定。氏名等はすべて Enter で飛ばしてよい。

```bash
usermod -aG sudo app
```

### 2-3. ファイアウォール

```bash
ufw allow OpenSSH
ufw allow 80
ufw allow 443
ufw --force enable
ufw status
```

`80/tcp ALLOW` `443/tcp ALLOW` `22/tcp ALLOW` が並んでいればOK。
**アプリの3000番は開けない**（外部にはNginx経由でのみ公開するため）。

### 2-4. swapを作る（1GBメモリでは必須）

ビルド時のメモリ不足を防ぐため、2GBのswapを追加する。

```bash
fallocate -l 2G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile
echo '/swapfile none swap sw 0 0' >> /etc/fstab
free -h
```

`Swap: 2.0Gi` と表示されればOK。

---

## 3. Node.js をインストール

Node 24系を入れる（開発環境と揃える）。

```bash
curl -fsSL https://deb.nodesource.com/setup_24.x | bash -
apt install -y nodejs git
node -v
npm -v
```

`v24.x.x` が表示されればOK。

---

## 4. アプリのファイルをサーバーに置く

### 方法A: GitHub経由（推奨・更新が楽）

**手元のPC**で、まだ一度もコミットしていないのでコミットしてpushする。
GitHubで**プライベートリポジトリ**を作ってから:

```powershell
cd "C:\Users\shimadayuma\Desktop\Cload Code\画像サーチ\bluearchive-search"
git add -A
git commit -m "初回コミット"
git branch -M main
git remote add origin https://github.com/【要変更:あなたのID】/irodori-art-archive.git
git push -u origin main
```

> `git branch -M main` は、ローカルのブランチ名が `master` のため GitHub の既定名 `main` に合わせるもの。
>
> **`.gitignore` で `/data/`（SQLiteのDB）と `.env*` を除外済み。**
> DBを含めると、本番サーバーで `git pull` したときに**公開中のデータベースが上書きされて消える**ため、絶対に含めないこと。
> DBはサーバー上で自動生成される（生徒マスタも自動投入される）。

**サーバー側**で取得:

```bash
su - app
git clone https://github.com/【要変更:あなたのID】/irodori-art-archive.git app
cd app
```

### 方法B: 直接転送（GitHubを使わない場合）

**手元のPC**の PowerShell で実行（`node_modules` と `.next` は転送しない）:

```powershell
cd "C:\Users\shimadayuma\Desktop\Cload Code\画像サーチ"
tar -czf app.tar.gz --exclude=node_modules --exclude=.next --exclude=.next-broken-backup bluearchive-search
scp app.tar.gz root@153.122.4.92:/home/app/
```

**サーバー側**:

```bash
cd /home/app
tar -xzf app.tar.gz
mv bluearchive-search app
chown -R app:app /home/app/app
su - app
cd app
```

---

## 5. 環境変数を設定

`app` ユーザーで、アプリのディレクトリにいる状態で実行。

```bash
nano .env.local
```

以下を貼り付け、値を変更する。

```
ADMIN_KEY=【要変更:長いランダムな文字列に】
NEXT_PUBLIC_DLSITE_AFF_ID=【要変更:DLsiteのアフィリID。未取得なら行ごと消す】
```

保存は `Ctrl+O` → `Enter`、終了は `Ctrl+X`。

> **ADMIN_KEY は必ず変更すること。** 未設定だと `change-me` になり、管理画面が誰でも開けてしまう。
> ランダム文字列は `openssl rand -base64 32` で生成できる。
>
> ⚠️ **このファイルはGitHubにpushされるので、実際のADMIN_KEYやアフィリIDをここに書かないこと。**
> 値はサーバー上の `.env.local`（gitignore済み）にだけ書く。

---

## 6. ビルドして起動

```bash
npm ci
npm run build
```

ビルドは1GB環境だと数分かかる。swapを設定済みなら通るはず。

PM2（プロセス常駐ツール）で起動する。

```bash
sudo npm install -g pm2
pm2 start npm --name irodori -- start
pm2 save
pm2 status
```

`online` と表示されればアプリは起動している。

**サーバー再起動時に自動で立ち上がるようにする:**

```bash
pm2 startup
```

→ 表示された `sudo env PATH=...` の行を**そのままコピーして実行**する。その後もう一度:

```bash
pm2 save
```

動作確認:

```bash
curl -I http://localhost:3000
```

`HTTP/1.1 200 OK` が返ればOK。

> **注意**: レート制限がメモリ内実装のため、**PM2のクラスタモード（`-i` オプション）は使わないこと**。単一プロセスで動かす。

---

## 7. Nginx（リバースプロキシ）を設定

外部からの80/443番を、アプリの3000番に転送する。

```bash
exit          # appユーザーからrootに戻る
apt install -y nginx
nano /etc/nginx/sites-available/irodori
```

以下を貼り付け:

```nginx
server {
    listen 80;
    server_name irodori-art-archive.com www.irodori-art-archive.com;

    # 画像が多いので長めに
    client_max_body_size 10M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

有効化:

```bash
ln -s /etc/nginx/sites-available/irodori /etc/nginx/sites-enabled/
rm -f /etc/nginx/sites-enabled/default
nginx -t
systemctl restart nginx
```

`syntax is ok` / `test is successful` と出ればOK。

---

## 8. DNSをVPSに向ける

**mixhostの管理画面（cPanel）** で、ドメインのDNSゾーン編集を開き、Aレコードを設定する。

| ホスト名 | 種別 | 値 |
|---|---|---|
| `irodori-art-archive.com` | A | 153.122.4.92 |
| `www` | A | 153.122.4.92 |

- 既存のmixhostサーバーを指すAレコードがあれば**書き換える**
- 反映には数分〜数時間かかる

反映確認（手元のPCで）:

```powershell
nslookup irodori-art-archive.com
```

VPSのIPが返ってくれば反映完了。

> **mixhost解約時の注意**: 解約するとDNS管理画面も使えなくなる可能性がある。
> その場合はドメインを別のレジストラ（お名前.com等）に移管するか、Cloudflareのネームサーバーを使う。
> **解約前にサポートへ「ドメインは継続利用したい」と確認しておくこと。**

---

## 9. HTTPS化（Let's Encrypt・無料）

DNSが反映されてから実行する（反映前だと失敗する）。

```bash
apt install -y certbot python3-certbot-nginx
certbot --nginx -d irodori-art-archive.com -d www.irodori-art-archive.com
```

- メールアドレスを聞かれるので入力
- 規約に同意 `Y`
- HTTPリダイレクトは **`2`（リダイレクトする）** を選ぶ

証明書は自動更新される。確認:

```bash
systemctl status certbot.timer
```

---

## 10. 消滅ツイート検出を自動化（cron）

削除されたツイートを自動で非表示にする。

```bash
su - app
crontab -e
```

（エディタ選択が出たら `1`（nano）を選ぶ）
末尾に追加:

```
0 4 * * * cd /home/app/app && /usr/bin/node scripts/check-dead.mjs >> /home/app/check-dead.log 2>&1
```

毎日4時に実行される。

---

## 11. 公開前の最終チェック

- [ ] `https://irodori-art-archive.com` が表示される
- [ ] 鍵マーク（HTTPS）がついている
- [ ] `/admin` に**新しいADMIN_KEY**でログインできる（`change-me` で入れないことを確認）
- [ ] `/register` からイラストを登録できる
- [ ] モード切替（健全/きわどい/R18）が動く
- [ ] `/terms` の運営者情報・連絡先を記入した
- [ ] `/removal` の削除依頼フォームが動く
- [ ] `https://irodori-art-archive.com/robots.txt` と `/sitemap.xml` が表示される
- [ ] Google Search Console にサイトを登録し、sitemapを送信した

---

## 日常運用のコマンド

```bash
# アプリの状態を見る
pm2 status
pm2 logs irodori

# 再起動
pm2 restart irodori

# コードを更新して反映（GitHub経由の場合）
su - app
cd app
git pull
npm ci
npm run build
pm2 restart irodori

# メモリの空きを見る
free -h
```

---

## 困ったときは

| 症状 | 対処 |
|---|---|
| `npm run build` が固まる/落ちる | メモリ不足。`free -h` でswapが有効か確認（手順2-4） |
| 502 Bad Gateway | アプリが落ちている。`pm2 status` → `pm2 restart irodori` |
| certbotが失敗する | DNS未反映。`nslookup` でIPを確認してから再実行 |
| SSHで入れない | GMO管理画面の「コンソール」から直接ログインできる |
| 管理画面に入れない | `.env.local` の ADMIN_KEY を確認 → `pm2 restart irodori`（env変更後は再起動が必要） |
